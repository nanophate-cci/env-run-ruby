version: 2.1
jobs:
  build:
    docker:
      - image: cimg/python:stable
    steps:
      - checkout
      - run: 
          command: |
              print "aaa"
          name: hola
          shell: /usr/bin/env python
